import 'package:flutter/material.dart';

class LeasingProcessScreen extends StatelessWidget {
  const LeasingProcessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Leasing Process Screen',
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}